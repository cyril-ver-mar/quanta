"""L3 — thin DB placeholder (expand when checklist D1 = yes)."""

from __future__ import annotations

# Example only: open SQLite, run schema, expose repositories.
# See TASK_PLAYBOOKS/05_sqlite_and_backups.md
